import React from 'react';
import classes from './Settings.module.css';

const Settings =() => {
  return(
      <div className={classes.wrapper_settings}>
          Settings
      </div>
  )
};

export default Settings;